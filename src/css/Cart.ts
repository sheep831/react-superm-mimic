import styled from "styled-components";

export const Container = styled.div`
  background: var(--bkg-color);
  margin: 0 auto;
  max-width: 900px;
  padding-top: 40px;
  & img {
    border-radius: 30px;
  }
`;